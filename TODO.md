# TODO

## Outstanding items (2026-03-27)

1. ~~**GitHub Actions update**~~ — done, updated release body text
2. **Firebase hosting setup** — move web deployment from site.eu to Firebase Hosting; currently uses SSH/rsync via `deployment/` scripts
3. ~~**Competition name in Photo Placement toolbar**~~ — done, shows Chip with competition name
4. ~~**Cleanup modal in launcher**~~ — done, cleanup banner with age/excess detection
5. **Web standalone experience** — photo-helper works standalone with its own selector; map-corridors in web mode has no competition UI (flat session only)

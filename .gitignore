# Test photos and documentation
# `/photos/` only at repo root — the photo-map-culling test fixture dir
# at frontend/map-corridors/test/fixtures/photos/ is legitimately tracked.
/photos/
tmp/
TODO.md


# Playwright E2E artifacts
test-results/
playwright-report/
playwright/.cache/

# Dependencies
node_modules/
package-lock.json
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
# But our Electron desktop package has a `lib/` directory of pure
# CommonJS helpers (path validators, etc.) that DOES need to be tracked.
!frontend/desktop/lib/
!frontend/desktop/lib/**
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Build markers and artifacts
.deps_installed

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# ESLint cache
.eslintcache

# Build outputs
/build
/dist

# Local development files
/uploads
/temp
*.tmp

# Backend storage - runtime data, should not be in git
backend/storage/
backend/photo-helper/storage/

# Vite
.vite

# Project specific
.gitignore
ukazky/
deploy.conf
public_html

# Claude Code local settings (per-user permissions allowlist)
# and runtime lock files (e.g. scheduled_tasks.lock)
.claude/settings.local.json
.claude/*.lock

# Local PR diff dumps (e.g. pr47.diff, pr50_review.diff) saved during
# review sessions — never intended for the repo. Both plain and dot-prefixed
# scratch names (e.g. .pr64_p2.txt, .pr66.diff).
/pr*.diff
/.pr*.diff
/.pr*.txt

# Local scratch / scrap files dropped in the repo root during work sessions
# (raw patches, KML/photo exports, scratch docs) — not part of the project.
# RED.kml / RED_SC.kml are intentionally-tracked root corridor assets, so they
# are negated below; add any future legitimate root KML the same way.
/*.patch
/*.kml
!/RED.kml
!/RED_SC.kml
/*.docx
/RIMG*.JPG

# Root-level static deploy bundle (built output staged for hosting,
# distinct from per-app `dist/` already covered above)
/public/

# Backend photo-helper specific
backend/photo-helper/venv/
backend/photo-helper/.deps_installed
# Local sample competition (bundled into local builds, never committed)
frontend/desktop/sample-data/*
!frontend/desktop/sample-data/.gitkeep

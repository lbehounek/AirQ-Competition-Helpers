# FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# File handling and image processing
# Pillow 12.2.0 fixes CVE-2026-25990 (OOB write via PSD) + CVE-2026-40192 (FITS gzip decompression bomb)
Pillow==12.2.0
# python-multipart 0.0.22 fixes CVE-2026-24486 (arbitrary file write via path traversal)
python-multipart==0.0.22

# PDF generation
reportlab==4.0.7

# Data validation and serialization
pydantic==2.5.0

# CORS support for frontend
fastapi-cors==0.0.6

# File system utilities
aiofiles==23.2.1

# Unique ID generation (use Python stdlib uuid, no external package required)

# Date/time utilities
python-dateutil==2.8.2
